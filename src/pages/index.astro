<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Astro</title>
    <script type="module" src="./node_modules/htmx.org/dist/htmx.js"></script>
  </head>
  <body class="bg-gray-100 min-h-screen flex flex-col">
    <!-- Cabeçalho -->
    <header class="bg-blue-500 text-white py-4">
      <div class="container mx-auto flex justify-between items-center px-4">
        <h1 class="text-lg font-semibold">Astro</h1>
        <!-- Você pode adicionar links de navegação ou outros elementos aqui, se desejar -->
      </div>
    </header>

    <!-- Conteúdo Principal -->
    <main
      class="flex-grow container mx-auto flex flex-col justify-center items-center py-8"
    >
      <form
        hx-get="/api/search"
        hx-target="#search-results"
        class="flex flex-col md:flex-row gap-4 items-center bg-white p-8 rounded-lg shadow-md"
      >
        <input
          type="text"
          name="cep"
          placeholder="Enter a CEP"
          class="py-3 px-4 border border-gray-300 rounded-md focus:outline-none focus:border-blue-500 w-full md:w-auto"
          pattern="[0-9]{5}-[0-9]{3}"
        />
        <button
          type="submit"
          class="py-3 px-6 bg-blue-500 text-white rounded-md hover:bg-blue-600 focus:outline-none focus:bg-blue-600 transition duration-300"
          >Search</button
        >
      </form>
      <div id="search-results" class="mt-8 w-full md:w-1/2"></div>
    </main>

    <!-- Rodapé -->
    <footer class="bg-gray-800 text-white py-4 mt-auto">
      <div class="container mx-auto text-center">
        &copy; 2024 Astro. Todos os direitos reservados.
      </div>
    </footer>
  </body>
</html>
